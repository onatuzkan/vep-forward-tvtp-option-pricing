# Runtime dependencies for the forward-centered PDE option model.
# Pinned to the versions the current test suite (172 passed) is verified against.
numpy==1.26.4
pandas==2.3.3
scipy==1.16.2
matplotlib==3.10.7
PyYAML==6.0.3
# Required by pandas.DataFrame.to_markdown() (calibration_audit.md writer).
tabulate==0.9.0
