__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/
env/
.DS_Store
.vscode/settings.json
*.zip
*.local.md
scratch/
.claude/
scripts/dev_checks/*.local.*

.env

# ---- credential hygiene (added after eptr2-tgt leak, 2026-09-12) ----
.eptr2-tgt
*.tgt
*token*
*credential*
*secret*
*.pem
*.key

# ---- multi-date forward backtest: per-date hourly curves are regenerable
# (640 KB each) and are rebuilt by scripts/backtest/multi_date_forward_backtest.py.
# The raw EPİAŞ GGF JSONs (2.3 MB total) stay tracked: the anchor check in
# build_vep_quote_sets.py depends on them.
outputs/multi_date/*/hourly_forward_curve.csv
