__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
.venv/
venv/
env/
.DS_Store
.vscode/settings.json
*.zip
*.local.md
scratch/
.claude/
scripts/dev_checks/*.local.*

.env

# ---- credential hygiene (added after eptr2-tgt leak, 2026-09-12) ----
.eptr2-tgt
*.tgt
*token*
*credential*
*secret*
*.pem
*.key

# ---- multi-date forward backtest: per-date hourly curves are regenerable
# (640 KB each) and are rebuilt by scripts/backtest/multi_date_forward_backtest.py.
# The raw EPİAŞ GGF JSONs (2.3 MB total) stay tracked: the anchor check in
# build_vep_quote_sets.py depends on them.
outputs/multi_date/*/hourly_forward_curve.csv

# ---- scratch folder for ad-hoc report copies; the canonical artefacts live
# under outputs/.  A copy here goes stale as soon as the script is re-run.
Claude outputs/

# ---- Internal / AI-tooling artefacts that are kept locally but not public
# (untracked after the 2026 pre-public audit).
CLAUDE.md
docs/ChatGPT_Proje_Promptu.md
docs/Denetim_Raporu_VEP_TVTP_ve_Bayesci_Gelistirme.md
