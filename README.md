# Energy Markets Options Valuation

PDE-based European option pricer for the Turkish day-ahead market (EPİAŞ
PTF, TRY/MWh).  The price *level* is anchored to the EPİAŞ VEP monthly
baseload forward strip; the *residual* around that curve follows a
two-regime TVTP (time-varying transition probability) Markov-switching OU
process whose parameters are inherited from the M9 fit of the historical
hourly PTF series.  Every option value produced here is labelled
**"VEP-forward-curve anchored option prices"** — only the first moment is
market-identified; volatility and regime-transition risk premia are not.

## Repository layout

```
.
├── README.md                — this file
├── requirements.txt         — runtime deps (numpy, pandas, scipy, matplotlib, PyYAML)
├── requirements-dev.txt     — adds pytest for the test suite
├── run_pde.py               — CLI: validate | calibrate-market | price | diagnostics | freeze-params
├── scenario_sweep.py        — helper: ±2σ RD-offset sensitivity of the 72 h call
├── config/                  — YAML configs (default: forward_centered_config.yaml)
├── inputs/
│   ├── historical/          — frozen TVTP parameters (m2_frozen_parameters.yaml), z series
│   ├── historical/archive/  — raw M9 handoff bundle + placeholder-era yaml
│   ├── market/              — EPİAŞ VEP monthly quotes (CSV + JSON + schema)
│   └── legacy_reference/    — legacy sinh-Gaussian benchmark curve
├── pde_option_model/        — Python package (PDE solver, forward curve, TVTP generator, calibration)
├── tests/                   — pytest suite (169 passing)
├── outputs/                 — generated artefacts (regenerated by the CLI, safe to delete)
│   └── market_calibration_final/     — accepted calibration + audit + archived alternatives
├── docs/                    — methodology appendix + team manual
├── paper/                   — LaTeX manuscript (elsarticle), figures, and the compiled PDF
└── scripts/
    ├── dev_checks/          — one-shot dev verification scripts (superseded by the CLI)
    └── tvtp_derivation/     — TVTP alpha-derivation script (referenced by the methodology doc)
```

## Installation

```
python -m venv .venv
.venv/Scripts/activate                # Windows PowerShell / bash-for-Windows
# source .venv/bin/activate           # Linux / macOS
python -m pip install --upgrade pip
python -m pip install -r requirements-dev.txt
```

The requirements files are pinned to the versions the test suite is
verified against (numpy 1.26, pandas 2.3, scipy 1.16, matplotlib 3.10,
PyYAML 6.0, pytest 9.1).

## Running

```
# 1. Structural / numerical self-checks (no calibration, ~1 s)
python run_pde.py validate

# 2. Rebuild the accepted forward-curve calibration under the frozen yaml
python run_pde.py calibrate-market --model forward_centered \
    --outdir outputs/market_calibration_final

# 3. Price a 72 h European call at K = 3000 TRY/MWh (PDE + Monte Carlo cross-check)
python run_pde.py price --model forward_centered \
    --curve outputs/market_calibration_final \
    --option-type call --strike 3000 --maturity-hours 72

# 4. RD-scenario sweep (±2σ standardized RD offset, 9 rows)
python scenario_sweep.py

# 5. Full test suite (169 passed)
python -m pytest -q
```

### Optional: sensitivity to the regime-probability initial condition

The default `pi_filtered` on the frozen yaml is the M2-shipped filter
`[0.9320, 0.0680]`; a valuation-time M9 filter is not available.  Under
`--pi-override stationary` the pricer swaps in the M9 stationary
occupancy `[0.3254, 0.6746]` — impact <2 % on 72 h+ maturities, up to
~30 % on <24 h maturities.  See `model_limitations.md` item (e).

```
python run_pde.py price --model forward_centered \
    --curve outputs/market_calibration_final \
    --strike 3000 --maturity-hours 72 --pi-override stationary
```

## Calibration history

* The forward curve is calibrated exactly to the six-month VEP quote
  strip (Feb-Jul 2026); the January 2026 window is anchored by the
  production `spot_to_next_linear` rule (accepted after the anchor-mode
  review under `outputs/market_calibration_final/archive/near_term_anchor_review/`).
* TVTP volatilities and gamma slopes are the M9 fit values (regime-label
  swapped to the yaml convention).  The two `alpha` intercepts are
  **derived**, not MLE-estimated: they are back-solved from the reported
  M9 mean-duration diagnostics via `scripts/tvtp_derivation/derive_tvtp_parameters.py`.
  Full derivation and cross-check are in
  [`docs/tvtp_derivation_methodology.md`](docs/tvtp_derivation_methodology.md).
* Every acceptance / provenance caveat is auto-included in the
  regenerated `outputs/market_calibration_final/model_limitations.md`
  (items (a)-(e)).

## What is *not* identified without option premia

Volatility risk premium, regime-transition premia η₀₁ / η₁₀, and any
regime-specific market price of risk λᵢ are not identifiable from
forward quotes alone.  See the "4_not_identified_without_option_prices"
block in `outputs/market_calibration_final/parameter_identification.json`
and the pricing-label warning in every emitted YAML/JSON.

## Data

The market data used in this repository is taken from the **EPİAŞ
Şeffaflık Platformu** (Energy Market Transparency Platform,
https://seffaflik.epias.com.tr/), which publishes Turkish electricity
market data under an EPDK (Enerji Piyasası Düzenleme Kurumu) Board
Decision.  The relevant files under `inputs/market/` and
`inputs/historical/` were re-published here for the sole purpose of
reproducing the accompanying manuscript's calibration, backtest, and
sensitivity results.  Their original terms of use continue to apply;
see the "Raw EPİAŞ data" section of [`LICENSE`](LICENSE) and
https://seffaflik.epias.com.tr/ for the current Terms of Use.

EPİAŞ is the original publisher of this data and is not responsible
for the accuracy, completeness, or interpretation of the data as
re-published or transformed here.  Any errors introduced by the
preparation scripts (`scripts/data/download_epias.py` and the
downstream feature builders) are the authors' responsibility, not
EPİAŞ's.

Users who wish to redistribute or reuse the raw EPİAŞ-sourced files,
or any derivative thereof, should consult
https://seffaflik.epias.com.tr/ directly.

## License

Four-part licensing; see [`LICENSE`](LICENSE) for the full text:

* **Code** — MIT.  Everything under `pde_option_model/`, `scripts/`,
  `tests/`, `paper/make_figures.py`, `paper/make_pde_mc.py`,
  `run_pde.py`, `scenario_sweep.py`.
* **Manuscript (`paper/`)** — CC BY-NC-ND 4.0, matching the SSRN
  release.  Applies to the LaTeX source (`main.tex`, `refs.bib`,
  `sections/*.tex`, `figures/*.pdf`, `figures/pipeline_src.tex`), the
  compiled PDF, and `paper/README.md`.  The regeneration scripts
  `paper/make_figures.py` and `paper/make_pde_mc.py` themselves fall
  under the MIT code licence, not CC BY-NC-ND — the NoDerivatives
  restriction is on the manuscript as a scholarly artefact, not on the
  tooling that rebuilds it.  Repository and SSRN versions of the paper
  are identical.
* **Documentation and derived data** — CC BY 4.0.  Everything under
  `docs/`, every `.md` under `outputs/`, every derived CSV / JSON /
  YAML / PNG under `outputs/`, and
  `inputs/historical/m2_frozen_parameters.yaml`.  When citing please
  reference the accompanying preprint (see the Manuscript section
  below).
* **Raw EPİAŞ data** — under the EPİAŞ Şeffaflık Platformu Terms of
  Use.  See the "Data" section below.

## Manuscript

The preprint accompanying this repository:

* **Title:** *Anchoring Options to an Incomplete Forward Curve: Identification and Out-of-Sample Evidence from Turkish Electricity*
* **Authors:** Onat Uzkan (Özyeğin University) and Koray Ömercan Saçlı (Istanbul Technical University)
* **Class:** `elsarticle` (Elsevier preprint), pdfLaTeX; 10 body sections + 3 appendices, ~9 750 words, 11 figures, 6 tables

The manuscript source and compiled PDF live under [`paper/`](paper/):

```
paper/
├── main.tex                                  document class + section includes
├── refs.bib                                  bibliography (42 entries, all cited)
├── sections/                                 01-introduction … 10-conclusion + A-C
├── figures/                                  publication PDFs used by main.tex
├── make_figures.py                           regenerates every data figure from outputs/
├── make_pde_mc.py                            PDE / Monte Carlo verification sweep
├── README.md                                 build instructions + PDF naming convention
└── Uzkan_Sacli_2026_forward_anchored_option_valuation_v1.pdf
```

### Compiling in Overleaf

1. Upload `paper/` as a new project (`Menu → Upload Project → zip`).
2. `Menu → Compiler: pdfLaTeX`; main document `main.tex`.
3. Recompile twice — BibTeX runs between passes.

Zero errors, zero undefined references, zero overfull boxes on the shipped source.  The compiled artefact naming convention (`Uzkan_Sacli_2026_forward_anchored_option_valuation_v<N>.pdf`) is documented in `paper/README.md`; bump `<N>` on every material revision.

### Reproducibility — every manuscript artefact traces to `outputs/`

Every data figure and every empirical table in the manuscript is produced from a tracked artefact under `outputs/` or `inputs/`.  Rebuild any of them with `paper/make_figures.py` (all data figures) or `paper/make_pde_mc.py` (Figure 4, the PDE vs Monte Carlo verification), then recompile `main.tex`.

| section / figure | filename | data source |
|---|---|---|
| Fig 1 — pipeline schematic | `figures/pipeline.pdf` | TikZ standalone: `figures/pipeline_src.tex` |
| Fig 2 — hourly VEP forward curve | `figures/fwd_curve.pdf` | `outputs/market_calibration_final/hourly_forward_curve.csv` + `inputs/market/vep_monthly_quotes.csv` |
| Fig 3 — TVTP transition mechanism | `figures/tvtp_mechanism.pdf` | `inputs/historical/m2_frozen_parameters.yaml` (TVTP α/γ) + `inputs/historical/rd_standardized.csv` (empirical z) |
| Fig 4 — PDE vs Monte Carlo | `figures/pde_mc.pdf`, `.json` | live `run_pde.py price` sweep in `paper/make_pde_mc.py` |
| Fig 5 — option-price surface | `figures/option_surface.pdf` | `outputs/market_calibration_final/strike_maturity_grid.csv` |
| Fig 6 — residual std vs horizon | `figures/residual_sd.pdf` | `outputs/forward_centered_diagnostics/residual_diagnostics.csv` |
| Fig 7 — anchor-level sensitivity | `figures/anchor_sensitivity.pdf` | `outputs/market_calibration_final/near_term_anchor_sensitivity.csv` |
| Fig 8 — legacy vs forward-centered | `figures/legacy_vs_centered.pdf` | `outputs/market_calibration_final/legacy_vs_forward_centered.csv` |
| Fig 9 — RD scenario sweep | `figures/rd_sweep.pdf` | `outputs/scenario_sweep/rd_scenario_sweep.csv` |
| Fig 10 — 2026 realised backtest | `figures/backtest.pdf` | `inputs/market/realized_ptf_2026.csv` + `outputs/market_calibration_final/realized_2026_backtest.monthly.csv` |
| Fig 11 — multi-date forward errors | `figures/multi_date.pdf` | in-script summary (rebuilt by `scripts/backtest/multi_date_forward_backtest.py`) |
| Table 1 — VEP quotes at valuation | (in `sections/02-market-data.tex`) | `inputs/market/vep_monthly_quotes.csv` + `inputs/market/vep_2025-12-31.json` |
| Table 2 — production parameters + provenance | (in `sections/06-provenance.tex`) | `inputs/historical/m2_frozen_parameters.yaml` + `outputs/market_calibration_final/parameter_identification.json` |
| Table 3 — option-value grid | (in `sections/07-results.tex`) | `outputs/market_calibration_final/strike_maturity_grid.csv` |
| Table 4 — sensitivity summary | (in `sections/07-results.tex`) | scenario sweep + anchor sensitivity + risk-premium sensitivity CSVs |
| Table 5 — realised evaluation | (in `sections/07-results.tex`) | `outputs/market_calibration_final/realized_2026_backtest.monthly.csv` |
| Table 6 — multi-date bias | (in `sections/07-results.tex`) | in-script (see Fig 11) |
